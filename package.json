{
  "name": "4d-agent-office-netlify",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "test": "node --test tests/*.test.mjs", "build": "node --check public/app.js" },
  "dependencies": { "@netlify/blobs": "^10.0.0" }
}
